quarkus:
  datasource:
    db-kind: mysql
    username: root
    password: admin
    jdbc:
      url: jdbc:mysql://localhost:3306/peoples

  hibernate-orm:
    schema-management:
      strategy: drop-and-create

  redis:
    hosts: "dummy_host:6379"
    password: "dummy_key"
    ssl: true